name: Test Suite

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'config.yaml' # Config changes handled by smoke-test.yml

# Per FR-037a: Explicit least-privilege permissions
permissions:
  contents: read

jobs:
  test:
    name: Run All Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      # Uses composite action that includes actions/cache for pnpm dependencies
      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Run type checking
        run: pnpm run type-check
        continue-on-error: false

      - name: Run linting
        run: pnpm run lint
        continue-on-error: false

      - name: Run unit tests
        run: pnpm run test:unit
        continue-on-error: false

      # Cache Playwright browsers to speed up CI builds (issue #33)
      # Cache key is based on Playwright version from package.json for proper invalidation
      - name: Get installed Playwright version
        id: playwright-version
        run: |
          PLAYWRIGHT_VERSION=$(node -e "console.log(require('./package.json').devDependencies['@playwright/test'])")
          echo "PLAYWRIGHT_VERSION=$PLAYWRIGHT_VERSION" >> $GITHUB_ENV
          echo "Playwright version: $PLAYWRIGHT_VERSION"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ env.PLAYWRIGHT_VERSION }}

      # Install browsers only if cache miss; always install OS dependencies
      - name: Install Playwright browsers
        run: pnpx playwright install --with-deps
        if: steps.playwright-cache.outputs.cache-hit != 'true'

      - name: Install Playwright OS dependencies
        run: pnpx playwright install-deps
        if: steps.playwright-cache.outputs.cache-hit == 'true'

      - name: Generate test health data
        run: timeout 90 pnpm run check-once || true
        continue-on-error: false
        env:
          NODE_ENV: test

      - name: Build application
        run: pnpm run build:inline
        continue-on-error: false
        env:
          NODE_ENV: test

      - name: Run e2e tests
        run: pnpm run test:e2e
        continue-on-error: false

      # Accessibility testing is covered by E2E tests (axe-core integration)
      # Standalone accessibility tests directory is empty - uncomment when tests are added
      # - name: Run accessibility tests
      #   run: pnpm run test:accessibility
      #   continue-on-error: false

      # Performance testing directory is empty - uncomment when tests are added
      # - name: Run performance tests
      #   run: pnpm run test:performance
      #   continue-on-error: false

      - name: Run all tests with coverage
        run: pnpm test
        continue-on-error: false

      - name: Check coverage threshold (80% minimum per FR-040a)
        run: |
          # TECHNICAL DEBT: Temporary threshold reduction (see vitest.config.ts)
          # Target: 80% (constitutional requirement)
          # Current: 53% (2025-10-26 baseline)
          # Deadline: 2025-11-17 for full compliance

          CURRENT_DATE=$(date +%Y-%m-%d)
          GRACE_PERIOD_END="2025-11-17"

          if [ -f coverage/coverage-summary.json ]; then
            COVERAGE=$(node -e "console.log(JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json')).total.lines.pct)")
            echo "Code coverage: ${COVERAGE}%"

            # Determine threshold based on grace period
            if [[ "$CURRENT_DATE" < "$GRACE_PERIOD_END" ]]; then
              THRESHOLD=53
              echo "⚠️  GRACE PERIOD ACTIVE until $GRACE_PERIOD_END"
              echo "📊 Current threshold: ${THRESHOLD}% (temporary)"
              echo "🎯 Target threshold: 80% (constitutional requirement)"
              echo ""
              echo "Remediation milestones:"
              echo "  - 2025-11-02: 60% coverage"
              echo "  - 2025-11-09: 70% coverage"
              echo "  - 2025-11-17: 80% coverage (REQUIRED)"
            else
              THRESHOLD=80
              echo "✅ Grace period ended - enforcing constitutional 80% requirement"
            fi

            if (( $(echo "$COVERAGE < $THRESHOLD" | bc -l) )); then
              echo "❌ ERROR: Code coverage ${COVERAGE}% is below ${THRESHOLD}% threshold (FR-040a)"
              exit 1
            fi

            echo "✅ Code coverage meets ${THRESHOLD}% threshold"
          else
            echo "⚠️  Coverage file not found - skipping threshold check"
          fi

      - name: Upload coverage reports
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: coverage-reports
          path: coverage/
          retention-days: 30

      - name: Upload Playwright test results
        uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
